<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Level" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Level" parent="Base.Theme.Level" />
</resources>